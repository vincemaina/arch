font=JetBrainsMono Nerd Font:size=10
pad=8x8

# Catppuccin Mocha, which foot ships. This replaces an include of
# tokyonight-night, a theme foot does not ship: the path resolved to nothing,
# foot fell back to its default colours, and nothing reported the problem.
#
# Mocha because the bar was already mostly using that palette, so this is the
# cheapest way to make two components agree. TASK-3 decides the palette for the
# whole desktop and may replace this include with one of our own.
include=/usr/share/foot/themes/catppuccin-mocha
