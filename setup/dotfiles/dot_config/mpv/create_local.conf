# mpv settings for this machine only.
#
# Created once and never rewritten; nothing here is tracked. Included LAST, and
# mpv takes the last value it reads, so anything here overrides the repository's
# mpv.conf.
#
#     volume=70
#     ytdl-format=bestvideo[height<=?1080]+bestaudio/best
#
# Check what mpv resolved with:  mpv --show-profile=default
