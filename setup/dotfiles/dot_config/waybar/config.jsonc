// Waybar.
//
// Trimmed to what is worth a permanent place on screen. Removed: cpu, memory
// and temperature, which btop reports better on the rare occasions they matter
// and which otherwise just animate; the idle inhibitor and scratchpad
// indicators, which report state that is already obvious; and the backlight,
// which shows nothing on a machine without one.
//
// Also removed a large amount of configuration for modules that were never
// displayed at all - mpd, a media widget, a power menu, keyboard state, a
// second battery, power profiles - which made the file look far busier than
// the bar ever was.
{
    "layer": "top",
    "position": "top",
    "spacing": 4,

    "modules-left": ["sway/workspaces", "sway/mode"],
    "modules-center": ["sway/window"],
    "modules-right": ["pulseaudio", "battery", "clock", "tray"],

    "sway/workspaces": {
        "disable-scroll": true,
        "format": "{name}"
    },

    "sway/mode": {
        "format": "{}"
    },

    "sway/window": {
        "max-length": 60,
        "tooltip": false
    },

    "pulseaudio": {
        "format": "{icon} {volume}%",
        "format-muted": "muted",
        "format-icons": { "default": ["", "", ""] },
        "on-click": "pavucontrol",
        "tooltip": false
    },

    "battery": {
        "states": { "warning": 30, "critical": 15 },
        "format": "{capacity}% {icon}",
        "format-charging": "{capacity}% ",
        "format-icons": ["", "", "", "", ""],
        "tooltip": false
    },

    "clock": {
        "format": "{:%H:%M}",
        "format-alt": "{:%a %d %b}",
        "tooltip-format": "<tt>{calendar}</tt>"
    },

    "tray": {
        "spacing": 10
    }
}
