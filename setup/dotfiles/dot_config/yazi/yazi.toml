# yazi, the file manager on $mod+e.
#
# The only thing configured here is which editor opens a file, and it is worth
# explaining why that needs configuring at all.
#
# yazi's default text opener is literally `${EDITOR:-vi} %s`. That is a sensible
# default and it failed here in the least helpful way: EDITOR was unset, so it
# fell back to `vi`, which is not installed - nothing owns /usr/bin/vi on this
# machine - and pressing enter on a file reported "command vi not found".
#
# EDITOR is now set, in environment.d. This does not rely on that.
#
# WHY NOT JUST RELY ON EDITOR
#
# Because the environment is the fragile part. environment.d is read when the
# systemd user manager starts, and the user manager survives a logout - so a
# value added there is not in the session until the *next* login, and every
# process already running keeps the environment it started with. yazi inherits
# from foot, which inherits from sway, which was started before the variable
# existed. Naming the editor here means the file manager works from the moment
# the config is applied, rather than after a login nobody remembers is needed.
#
# It also means the two places that decide what edits a file agree: this, and
# the mimeapps association that sends text/plain to nvim.desktop. If the editor
# ever changes, both move together - and checks/session.sh checks the second.

[opener]
edit = [
    { run = 'nvim "$@"', desc = "nvim", block = true, for = "unix" },
]

# Opening a directory gives a shell in it.
#
# yazi's own default rule for directories is
#
#     { url = "*/", use = [ "edit", "open", "reveal" ] }
#
# with `edit` first. That was harmless while `edit` was ${EDITOR:-vi} and EDITOR
# was unset, because it failed; the moment `edit` was pointed at a working nvim,
# opening a directory started opening the directory *in the editor*. Fixing one
# thing revealed the other, which is why this rule exists rather than leaving
# yazi's default alone.
#
# -D "$1" and not a bare `foot`. A bare foot inherits yazi's working directory,
# which is the folder you are *in* - not the folder you are hovering over. So
# opening a subdirectory would have given a terminal one level above it, which
# is subtly wrong in the way that wastes a minute every time.
#
# "$1" is the first path yazi passes, the same substitution the edit rule above
# uses. Quoted, because directories have spaces in them.
#
# orphan so the terminal outlives yazi. Without it, closing the file manager
# takes the shell with it - the opposite of the point, since the reason to want
# a terminal here is to do something that lasts longer than the look around that
# prompted it. Confirmed as a real opener field: yazi's own built-in defaults
# use `orphan = true` on the rule that opens a video player.
open-terminal = [
    { run = 'foot -D "$1"', desc = "terminal here", orphan = true, for = "unix" },
]

[open]
prepend_rules = [
    { url = "*/", use = [ "open-terminal", "reveal" ] },
]
